# ============================================
# Variables de Entorno - Traductor Emberá-Español
# ============================================
# 
# Copia este archivo como .env y configura tus valores
# 
# Para localhost:
#   cp .env.example .env
#
# ============================================

# ============================================
# CONFIGURACIÓN DE LA BASE DE DATOS
# ============================================

# URL completa de conexión a PostgreSQL
# Formato: postgresql://usuario:password@host:puerto/nombre_base_datos
DATABASE_URL=postgresql://embera_user:tu_password@localhost:5432/embera_traductor

# Variables individuales de PostgreSQL
PGHOST=localhost
PGPORT=5432
PGUSER=embera_user
PGPASSWORD=tu_password
PGDATABASE=embera_traductor

# ============================================
# CONFIGURACIÓN DEL SERVIDOR
# ============================================

# Puerto en el que corre la aplicación
# Por defecto: 5000
# Para localhost se recomienda: 3000
PORT=3000

# Entorno de ejecución (development | production)
NODE_ENV=development

# ============================================
# SEGURIDAD
# ============================================

# Secreto para las sesiones (string aleatorio largo y seguro)
# IMPORTANTE: Genera un valor único y seguro para producción
# 
# Para generar uno aleatorio:
# - Linux/Mac: openssl rand -base64 32
# - Node.js: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=cambiar_por_un_string_aleatorio_muy_largo_y_seguro

# ============================================
# NOTAS IMPORTANTES
# ============================================
#
# 1. NUNCA subas el archivo .env a Git (ya está en .gitignore)
# 2. En producción, usa valores diferentes y más seguros
# 3. SESSION_SECRET debe ser único en cada instalación
# 4. Asegúrate de que PostgreSQL esté corriendo antes de iniciar la app
#
# ============================================
