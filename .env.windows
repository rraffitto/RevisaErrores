# ============================================
# Configuración para Windows 11
# ============================================
# Copia este archivo a .env y configura tus valores

# Puerto del servidor (3000 por defecto)
PORT=3000

# ============================================
# Configuración de PostgreSQL
# ============================================
# Reemplaza "TU_CONTRASEÑA" con la contraseña que elegiste al instalar PostgreSQL

DATABASE_URL=postgresql://postgres:TU_CONTRASENA@localhost:5432/traductor_embera
PGHOST=localhost
PGPORT=5432
PGUSER=postgres
PGPASSWORD=TU_CONTRASENA
PGDATABASE=traductor_embera

# ============================================
# Secreto para sesiones
# ============================================
# Genera un secreto seguro con:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"

SESSION_SECRET=cambiar_esto_por_un_secreto_aleatorio_seguro

# ============================================
# Modo de desarrollo
# ============================================
NODE_ENV=development

# ============================================
# Notas importantes para Windows:
# ============================================
# 1. No uses comillas en los valores
# 2. Reemplaza TU_CONTRASEÑA con tu contraseña real de PostgreSQL
# 3. Genera un SESSION_SECRET único y seguro
# 4. Asegúrate de que PostgreSQL esté corriendo antes de iniciar el servidor
# 5. Este archivo NO debe subirse a Git (ya está en .gitignore)
